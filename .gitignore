__pycache__/
*.pyc
.venv/
/archive/
.pytest_cache/
.DS_Store

# 개인 설정 — docker-compose.yml 위에 덮어쓰는 로컬 전용 파일 (시크릿 커밋 방지)
docker-compose.override.yml
.env
# 레거시 — 예전 compose.example.yaml 복사 패턴 잔재
compose.yaml
compose.override.yaml

# SvelteKit SPA 빌드 산출물 — Docker/CI 가 빌드해 동봉(소스 커밋 안 함)
chunchugwan/web/frontend_dist/

# 패키지 빌드 산출물 / 릴리스 SBOM (CI 가 생성해 릴리스에 첨부 — 커밋 안 함)
/dist/
*.cdx.json

# fablize 골 트래커 상태
.fablize/

# PyCharm 로컬 전용 워크스페이스 설정 (절대 경로·개인 설정 포함)
.idea/workspace.xml
.idea/.name

# 개인 로컬 권한 오버라이드 (부트스트랩 표준 §5.3)
.claude/settings.local.json
# 부록 A(GHA 비용 감사) 일회성 태스크 산출물 — 리포에 커밋하지 않음
gha-cost-audit.md
gha-settings-checklist.md
