__pycache__/
*.pyc
.venv/
/archive/
.pytest_cache/
.DS_Store

# 개인 설정 — compose.example.yaml 을 복사해 만든 로컬 파일
compose.yaml
compose.override.yaml
.env

# SvelteKit SPA 빌드 산출물 — Docker/CI 가 빌드해 동봉(소스 커밋 안 함)
chunchugwan/web/frontend_dist/

# fablize 골 트래커 상태
.fablize/

# PyCharm 로컬 전용 워크스페이스 설정 (절대 경로·개인 설정 포함)
.idea/workspace.xml
